{
  "name": "@catalog/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "generate": "prisma generate",
    "validate": "prisma validate",
    "format": "prisma format",
    "migrate": "prisma migrate dev",
    "constraints": "psql \"$DATABASE_URL\" -f sql/0001_constraints.sql",
    "test": "echo no tests",
    "typecheck": "echo ok",
    "build": "echo ok",
    "check:offline": "node scripts/check-schema.mjs"
  },
  "dependencies": {
    "@prisma/client": "7.9.1"
  },
  "devDependencies": {
    "prisma": "7.9.1"
  }
}
